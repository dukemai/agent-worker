{
  "name": "@agent/playwright-tools",
  "version": "0.1.0",
  "private": true,
  "description": "Browser automation for promotion scraping (ICA, etc.) — runs outside the Worker",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:ui": "playwright test --ui",
    "download-watchlist": "node scripts/download-promo-watchlist.mjs",
    "test:match-rules": "playwright test tests/match-promotions.spec.ts"
  },
  "devDependencies": {
    "@playwright/test": "^1.51.0",
    "@types/node": "^20",
    "typescript": "^5.5.0"
  }
}
